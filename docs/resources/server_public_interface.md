---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "vcp_server_public_interface Resource - vcp"
subcategory: ""
description: |-
  Manages a server's external (public) network interface. Changing server_id forces a new resource; bandwidth_mbps is updated in place.
---

# vcp_server_public_interface (Resource)

Manages a server's external (public) network interface. Changing `server_id` forces a new resource; `bandwidth_mbps` is updated in place.

## Example Usage

```terraform
# Adds an external (public) interface to a server with the given uplink bandwidth.
resource "vcp_server_public_interface" "example" {
  server_id      = vcp_server.example.id
  bandwidth_mbps = 80
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `bandwidth_mbps` (Number) Uplink bandwidth in Mbps (must be a multiple of 10).
- `server_id` (String) ID of the server this interface belongs to. Changing this forces a new resource.

### Read-Only

- `gateway` (String) Gateway address (computed).
- `id` (Number) NIC ID.
- `ip_address` (String) Public IP address (computed).
- `mac` (String) MAC address (computed).
- `mask` (Number) Network mask (computed).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Server public interfaces can be imported as 'server_id:nic_id'.
terraform import vcp_server_public_interface.example <server_id>:<nic_id>
```
