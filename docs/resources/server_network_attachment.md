---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "vcp_server_network_attachment Resource - vcp"
subcategory: ""
description: |-
  Attaches a server to an isolated network. All fields force a new resource (the API recreates the NIC when the network or IP changes).
---

# vcp_server_network_attachment (Resource)

Attaches a server to an isolated network. All fields force a new resource (the API recreates the NIC when the network or IP changes).

## Example Usage

```terraform
# Connects a server to an isolated network. ip_address is optional — if omitted,
# an address is assigned automatically.
resource "vcp_server_network_attachment" "example" {
  server_id  = vcp_server.example.id
  network_id = vcp_network.example.id
  ip_address = "10.100.0.11"
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `network_id` (String) ID of the isolated network. Changing this forces a new resource.
- `server_id` (String) ID of the server. Changing this forces a new resource.

### Optional

- `ip_address` (String) IP address on the network. Auto-assigned if not specified. Changing this forces a new resource.

### Read-Only

- `id` (Number) NIC ID.
- `mac` (String) MAC address (computed).
- `mask` (Number) Network mask (computed).

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Server network attachments can be imported as 'server_id:nic_id'.
terraform import vcp_server_network_attachment.example <server_id>:<nic_id>
```
