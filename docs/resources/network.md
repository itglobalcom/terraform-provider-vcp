---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "vcp_network Resource - vcp"
subcategory: ""
description: |-
  Manages an isolated network. Networks provide isolated networking environments for your infrastructure.
---

# vcp_network (Resource)

Manages an isolated network. Networks provide isolated networking environments for your infrastructure.

## Example Usage

```terraform
resource "vcp_network" "example" {
  name           = "production-network"
  location_id    = "kz"
  description    = "Production environment network"
  network_prefix = "10.100.0.0"
  mask           = 24

  tags = ["production", "managed-by-terraform"]
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `location_id` (String) The location ID where the network will be created. Changing this forces resource recreation.
- `name` (String) The name of the network.

### Optional

- `description` (String) A description of the network.
- `mask` (Number) The network mask (e.g., `24` for `/24`). If not specified, will be assigned automatically. **Changing this forces resource recreation.**
- `network_prefix` (String) The network prefix (e.g., `10.100.0.0`). If not specified, will be assigned automatically. **Changing this forces resource recreation.**
- `tags` (Set of String) If attribute is omitted, all existing tags will be removed.

### Read-Only

- `created` (String) The creation timestamp of the network in RFC3339 format.
- `gateway_ids` (List of String) List of gateway IDs attached to this network. This is a read-only attribute managed by the API.
- `id` (String) The unique identifier of the network. Used for referencing the network in other resources.
- `server_ids` (List of String) List of server IDs attached to this network. This is a read-only attribute managed by the API.

## Import

Import is supported using the following syntax:

The [`terraform import` command](https://developer.hashicorp.com/terraform/cli/commands/import) can be used, for example:

```shell
# Isolated networks can be imported by ID.
terraform import vcp_network.example <network_id>
```
